# zappa_cf_templates

Generic templates for creating a VPC in AWS, and deploying a Django/Flask/Bottle application via APIGateway and Lambda

Zappa is still used to build the package and tail logs, but these templates take the place of the deploy and certify commands.
